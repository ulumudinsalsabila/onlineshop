import Link from "next/link"; import { CheckCircleIcon, ClockCountdownIcon, WarningCircleIcon } from "@phosphor-icons/react/dist/ssr"; import { StorefrontShell } from "@/components/layout"; import { Container } from "@/components/shared/container"; import { MotionSection } from "@/components/shared/motion-section"; import { Button } from "@/components/ui/button"; import { PaymentActions } from "@/features/checkout/payment-actions"; import { authenticatedBackendApi } from "@/lib/authenticated-backend-api"; import { requireUser } from "@/lib/auth-guard"; import { formatIDR } from "@/lib/formatters";
type Order = { id: string; orderNumber: string; grandTotal: string | number; paymentStatus: string; payments: Array<{ provider: string }> };
export async function OrderStatusPage({ mode, searchParams }: { mode: "success" | "pending" | "failed"; searchParams: Promise<Record<string, string | string[] | undefined>> }) { await requireUser(); const query = await searchParams; const orderId = typeof query.orderId === "string" ? query.orderId : undefined; const orderNumber = typeof query.order === "string" ? query.order : undefined; let order: Order | null = null; try { const path = orderId ? `/orders/${encodeURIComponent(orderId)}` : `/orders/by-number/${encodeURIComponent(orderNumber ?? "")}`; order = (await authenticatedBackendApi<Order | null>(path, { cache: "no-store" })).data; } catch { /* invalid or expired link */ } const content = mode === "success" ? { title: "Payment successful", body: "Thank you. Your order has been paid and will be processed shortly.", icon: CheckCircleIcon } : mode === "failed" ? { title: "Payment unsuccessful", body: "The payment failed, was cancelled, or expired. Reserved stock has been released.", icon: WarningCircleIcon } : { title: "Awaiting payment", body: "We are waiting for secure confirmation from the payment provider. You can synchronise the status at any time.", icon: ClockCountdownIcon }; const Icon = content.icon; return <StorefrontShell><Container className="py-20"><MotionSection className="mx-auto max-w-2xl border border-border bg-secondary/25 p-8 text-center sm:p-12"><Icon className="mx-auto size-14 text-accent-foreground" weight="thin" aria-hidden /><p className="mt-6 text-xs tracking-[0.2em] uppercase">Payment status</p><h1 className="mt-3 font-serif text-4xl sm:text-5xl">{content.title}</h1><p className="mx-auto mt-4 max-w-lg text-sm leading-7 text-muted-foreground">{content.body}</p>{order ? <div className="mx-auto mt-7 max-w-sm border-y border-border py-5 text-sm"><div className="flex justify-between"><span>{order.orderNumber}</span><span>{formatIDR(Number(order.grandTotal))}</span></div><p className="mt-2 text-xs tracking-wider text-muted-foreground uppercase">{order.paymentStatus}</p></div> : <p className="mt-7 text-sm text-destructive">No order was found from this link.</p>}{order && mode === "pending" ? <PaymentActions orderId={order.id} mock={order.payments[0]?.provider === "mock"} /> : null}<div className="mt-8 flex flex-wrap justify-center gap-3"><Button asChild><Link href={order ? `/account/orders/${order.id}` : "/account/orders"}>View order</Link></Button><Button asChild variant="outline"><Link href="/products">Continue shopping</Link></Button></div></MotionSection></Container></StorefrontShell>; }
