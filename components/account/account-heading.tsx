export function AccountHeading({ eyebrow, title, description }: { eyebrow: string; title: string; description?: string }) { return <header className="border-b border-border pb-8"><p className="text-[0.625rem] font-semibold tracking-[0.18em] text-muted-foreground uppercase">{eyebrow}</p><h1 className="mt-3 font-serif text-(length:--text-heading-1) leading-none">{title}</h1>{description ? <p className="mt-4 max-w-2xl text-sm leading-7 text-muted-foreground">{description}</p> : null}</header>; }
