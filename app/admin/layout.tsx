import type { ReactNode } from "react"; import type { Metadata } from "next"; import { AdminShell } from "@/components/admin/admin-shell"; import { requireAdmin } from "@/lib/admin/auth"; import { authenticatedBackendApi } from "@/lib/authenticated-backend-api"; import { privateMetadata } from "@/lib/seo";
export const dynamic = "force-dynamic"; export const metadata: Metadata = { title: "Admin", ...privateMetadata };
export default async function AdminLayout({ children }: { children: ReactNode }) { const user = await requireAdmin(); const notifications = (await authenticatedBackendApi<{ pendingOrders: number; lowStock: number }>("/admin/views/layout", { cache: "no-store" })).data; return <AdminShell user={user} notifications={notifications}>{children}</AdminShell>; }
