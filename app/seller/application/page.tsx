import Link from "next/link";
import { ClockCountdownIcon, ShieldCheckIcon, WarningCircleIcon } from "@phosphor-icons/react/dist/ssr";
import { Button } from "@/components/ui/button";
import { requireSeller } from "@/lib/seller/auth";
export default async function SellerApplicationPage() { const { seller } = await requireSeller({ approved: false }); const Icon = seller.status === "APPROVED" ? ShieldCheckIcon : seller.status === "REJECTED" ? WarningCircleIcon : ClockCountdownIcon; return <section className="grid min-h-[65vh] place-items-center text-center"><div className="max-w-xl"><Icon className="mx-auto size-14 text-[#9d895c]" weight="thin" aria-hidden /><p className="mt-5 text-xs tracking-wider uppercase">Application status</p><h1 className="mt-2 font-serif text-5xl">{seller.status.replaceAll("_", " ")}</h1><p className="mt-4 text-sm leading-7 text-muted-foreground">{seller.status === "PENDING" ? "Tim kami sedang meninjau identitas dan informasi seller Anda." : seller.status === "APPROVED" ? "Profil seller disetujui. Anda dapat mulai membuat submission." : seller.rejectionReason ?? "Hubungi customer care untuk informasi lebih lanjut."}</p>{seller.status === "APPROVED" && <Button asChild className="mt-7"><Link href="/seller">Open seller dashboard</Link></Button>}</div></section>; }
